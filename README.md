## Important note

Some authentication and environment configuration files were intentionally removed or sanitized for security reasons.

To run the project locally, create a `.env` file based on `.env.example`.

Required environment variables:

```env
MASK_KEY=your_mask_key_here
